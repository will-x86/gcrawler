# Link policies


Current policies are:
- AllowAll
- AllowNone
- AllSameDomain ("same domain" grabs from seedURL's )
- GlobPolicy ( e.g. "*.example.com")
- MaxPerDomain ( e.g. max 2 ) 
- MaxDepth ( e.g. 1.com -> 2.com -> 3.com) depth = 2 here.
